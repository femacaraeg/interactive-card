import React from 'react';

import styles from './Card.module.css';

const Card = (props) => {
  return (
    <div className={styles.card}>
      <p>Jane Appleseed</p>
    </div>
  );
};

export default Card;
